# --- Основной пайплайн ---
numpy>=1.26,<2.0
pandas>=2.2
rasterio>=1.3
scipy>=1.13

# --- Rasterio требует системных GDAL/PROJ.
# На Windows ставится через wheels: pip install rasterio
# На Linux: apt install gdal-bin libgdal-dev перед pip